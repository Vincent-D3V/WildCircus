{% extends "base.html.twig" %}
{% block body %}
    <img src="https://cirquestarlight.ch/wp-content/uploads/2018/02/ti_contact.png" alt="contact" style="display: block; margin-left: auto; margin-right: auto; width: 300px; height: 150px; margin-bottom: 100px; margin-top: 100px; background-color: #00B8F5;">
    {{ form_start(contactForm) }}
    {{ form_errors(contactForm) }}
    <div class="ui input focus">
        {{ form_row(contactForm.name) }}
    </div>
    <div class="ui input focus">
        {{ form_row(contactForm.email) }}
    </div>
    {{ form_row(contactForm.message) }}

    <div class="ui button">
        <button style="display: flex; justify-content: center;" class="btn btn-outline-success" type="submit">Envoyé</button>
        {{ form_end(contactForm) }}
    </div>
    </div>
{% endblock %}